language: java
jdk:
  - oraclejdk8
sudo: required

services:
  - docker

install:
  - mvn clean compile -DskipTests
  - mvn test
  - mvn install -DskipTests -Pdefault,docker
  - mvn deploy -DskipTests -Pdefault,docker